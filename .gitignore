# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/tmp/
.playwright-mcp/
/screenshots/
# Playwright/browser screenshots dropped at the repo root. A bare filename
# passed to browser_take_screenshot lands here instead of the tool's output
# dir, so catch them at the root (nested /public/*.png etc. stay tracked).
/*.png

# deploy artifacts
nnf-ui.tar.gz
*.tar.gz
*.stackdump

# pdf.js runtime assets — generated by scripts/copyPdfjsAssets.mjs
/public/pdfjs/
